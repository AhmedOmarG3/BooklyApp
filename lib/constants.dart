import 'package:flutter/material.dart';

const kPraimaryColor = Color(0xff100B20);
const kduration = Duration(milliseconds: 250);
dynamic hieght(context) => MediaQuery.of(context).size.height;
dynamic width(context) => MediaQuery.of(context).size.width;
const String kFontFamily = 'gt sectra fine';